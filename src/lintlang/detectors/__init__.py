"""Detector implementations split out of `lintlang.patterns`.

Currently H1 only. H2-H7 still live in `lintlang.patterns`; the registry in
that module remains the single place where all seven are wired up.
"""
